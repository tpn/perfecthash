# perfecthash (Legacy Python Tree)

This directory contains the legacy in-repo Python package and support files.

It is not the supported packaging or release surface anymore. The active Python
package now lives at the repo root in `pyproject.toml` with source under
`python_src/perfecthash/`.

For the supported package and current build notes, see:

- `../README-python.md`
- `../docs/python-cli-and-api.md`
